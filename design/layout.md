# Layout 布局规范

## 整体结构

```
┌─────────────────────────────────────────────────────────────────┐
│  Top Bar (48px)                                                 │
├──────────┬──────────────────────────────────────────┬───────────┤
│          │                                          │           │
│  Live    │  ┌────────┬──────────────┬─────────┐    │ Inspector │
│  Preview │  │ Layers │   Canvas     │         │    │           │
│          │  │        │              │         │    │  240px    │
│  280px   │  │ 200px  │   flex-1     │         │    │           │
│          │  │        ├──────────────┤         │    │           │
│          │  │        │ Interaction  │         │    │           │
│          │  │        │   Manager    │         │    │           │
│          │  └────────┴──────────────┴─────────┘    │           │
└──────────┴──────────────────────────────────────────┴───────────┘
```

## 尺寸规范

### 固定尺寸

| 区域 | 宽度 | 说明 |
|------|------|------|
| Top Bar | 100% × 48px | 固定高度 |
| Live Preview | 280px | 可折叠 |
| Layers Panel | 200px | 可拖拽调整 |
| Inspector | 240px | 可折叠 |

### 弹性尺寸

| 区域 | 规则 |
|------|------|
| Canvas | `flex: 1`，填充剩余空间 |
| Interaction Manager | 高度 180-300px，可拖拽 |

---

## Top Bar

高度 48px，三段式布局。

```
┌─────────────┬─────────────────────────┬─────────────┐
│  Logo       │     Tool Bar            │   Actions   │
│  左对齐     │     居中                │   右对齐    │
└─────────────┴─────────────────────────┴─────────────┘
```

### 内容

- **左侧**: Logo + 项目名
- **中间**: 工具栏 (Select, Rectangle, Ellipse, Text, Hand)
- **右侧**: Undo/Redo, Help, Share, Preview

---

## Live Preview 面板

左侧固定宽度 280px。

### 结构

```
┌─────────────────────┐
│  Header             │
│  "LIVE PREVIEW"     │
├─────────────────────┤
│                     │
│  Device Frame       │
│  (iPhone mockup)    │
│                     │
├─────────────────────┤
│  Playback Controls  │
│  ▶️ ⏸️ 🔄           │
├─────────────────────┤
│  State Pills        │
│  [Idle] [Loading]   │
└─────────────────────┘
```

---

## Layers Panel

宽度 200px，Figma 风格图层树。

### 结构

```
┌─────────────────────┐
│  Search             │
├─────────────────────┤
│  ▼ Frame 1          │
│    ├─ Hero Card     │
│    ├─ Copy          │
│    └─ CTA Button    │
│  ▼ Frame 2          │
│    └─ ...           │
└─────────────────────┘
```

### 图层项样式

- 高度: 28px
- 缩进: 每级 16px
- 图标: 16×16px
- 选中态: `bg-active` + 左侧 2px accent 边框

---

## Canvas 区域

**多帧横向排列**，数量不固定，支持横向滚动。

### 整体结构

```
┌─────────────────────────────────────────────────────────────┐
│  Keyframe Strip (横向滚动)                                   │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌───  │
│  │  Idle   │  │ Loading │  │ Success │  │  Error  │  │ +   │
│  │         │  │         │  │         │  │         │  │     │
│  └─────────┘  └─────────┘  └─────────┘  └─────────┘  └───  │
├─────────────────────────────────────────────────────────────┤
│  Interaction Manager                                         │
│  [状态图] [时间轴]                                           │
└─────────────────────────────────────────────────────────────┘
```

### 关键帧卡片

```
┌─────────────────┐
│  Header         │
│  "Idle" ● ⋯     │
├─────────────────┤
│                 │
│   Canvas        │
│   360 × 640     │
│                 │
└─────────────────┘
```

### 尺寸规范

| 属性 | 值 | 说明 |
|------|-----|------|
| 卡片宽度 | 320px | 固定宽度 |
| 卡片高度 | 自适应 | 根据画布比例 |
| 卡片间距 | 20px | 卡片之间 |
| 容器内边距 | 24px | 左右留白 |

### 样式

- 背景: `bg-surface`
- 边框: `border-default`
- 圆角: `radius-lg` (8px)
- 选中态: `border-accent` + glow

### 横向滚动行为

```css
.keyframe-strip {
  display: flex;
  gap: 20px;
  padding: 24px;
  overflow-x: auto;
  scroll-snap-type: x proximity;
}

.keyframe-card {
  flex-shrink: 0;
  scroll-snap-align: start;
}
```

### 新增关键帧按钮

- 位置: 最右侧
- 样式: 虚线边框 + 加号图标
- 尺寸: 与卡片同高，宽度 80px

---

## Interaction Manager

Canvas 下方的交互管理区域，高度可拖拽调整。

### 结构

```
┌─────────────────────────────────────────────┐
│  [状态图]  [时间轴]              ▲ 拖拽调整  │
├─────────────────────────────────────────────┤
│                                             │
│   Tab 内容区域                               │
│                                             │
└─────────────────────────────────────────────┘
```

### 尺寸

| 属性 | 值 |
|------|-----|
| 最小高度 | 120px |
| 默认高度 | 200px |
| 最大高度 | 400px |

### Tab 切换

- **状态图**: 可视化状态机节点图
- **时间轴**: 关键帧时间线视图

---

## Inspector 面板

右侧固定宽度 240px。

### 结构

```
┌─────────────────────┐
│  Keyframe Info      │
│  Name / Summary     │
├─────────────────────┤
│  Transform          │
│  X Y W H            │
├─────────────────────┤
│  Fill / Stroke      │
├─────────────────────┤
│  Attributes         │
│  + Add              │
├─────────────────────┤
│  Transitions        │
└─────────────────────┘
```

### 属性行样式

- 行高: 32px
- 标签宽度: 60px
- 输入框: `flex: 1`
- 间距: 8px
