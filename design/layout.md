# Layout 布局规范

## 整体结构

```
┌─────────────────────────────────────────────────────────────────┐
│  Top Bar (48px)                                                 │
├──────────┬──────────────────────────────────────────┬───────────┤
│          │                                          │           │
│  Live    │  ┌────────┬──────────────┬─────────┐    │ Inspector │
│  Preview │  │ Layers │   Canvas     │         │    │           │
│          │  │        │              │         │    │  240px    │
│  280px   │  │ 200px  │   flex-1     │         │    │           │
│          │  │        ├──────────────┤         │    │           │
│          │  │        │ Interaction  │         │    │           │
│          │  │        │   Manager    │         │    │           │
│          │  └────────┴──────────────┴─────────┘    │           │
└──────────┴──────────────────────────────────────────┴───────────┘
```

## 尺寸规范

### 固定尺寸

| 区域 | 宽度 | 说明 |
|------|------|------|
| Top Bar | 100% × 48px | 固定高度 |
| Live Preview | 280px | 可折叠 |
| Layers Panel | 200px | 可拖拽调整 |
| Inspector | 240px | 可折叠 |

### 弹性尺寸

| 区域 | 规则 |
|------|------|
| Canvas | `flex: 1`，填充剩余空间 |
| Interaction Manager | 高度 180-300px，可拖拽 |

---

## Top Bar

高度 48px，三段式布局。

```
┌─────────────┬─────────────────────────┬─────────────┐
│  Logo       │     Tool Bar            │   Actions   │
│  左对齐     │     居中                │   右对齐    │
└─────────────┴─────────────────────────┴─────────────┘
```

### 内容

- **左侧**: Logo + 项目名
- **中间**: 工具栏 (Select, Rectangle, Ellipse, Text, Hand)
- **右侧**: Undo/Redo, Help, Share, Preview

---

## Live Preview 面板

左侧固定宽度 280px。

### 结构

```
┌─────────────────────┐
│  Header             │
│  "LIVE PREVIEW"     │
├─────────────────────┤
│                     │
│  Device Frame       │
│  (iPhone mockup)    │
│                     │
├─────────────────────┤
│  Playback Controls  │
│  ▶️ ⏸️ 🔄           │
├─────────────────────┤
│  State Pills        │
│  [Idle] [Loading]   │
└─────────────────────┘
```

---

## Layers Panel

宽度 200px，Figma 风格图层树。

### 结构

```
┌─────────────────────┐
│  Search             │
├─────────────────────┤
│  ▼ Frame 1          │
│    ├─ Hero Card     │
│    ├─ Copy          │
│    └─ CTA Button    │
│  ▼ Frame 2          │
│    └─ ...           │
└─────────────────────┘
```

### 图层项样式

- 高度: 28px
- 缩进: 每级 16px
- 图标: 16×16px
- 选中态: `bg-active` + 左侧 2px accent 边框

---

## Canvas 区域

三帧并排显示，类似 Principle。

### 关键帧卡片

```
┌─────────────────┐
│  Header         │
│  "Idle" ●       │
├─────────────────┤
│                 │
│   Canvas        │
│   360 × 640     │
│                 │
└─────────────────┘
```

- 卡片宽度: 360px (可缩放)
- 卡片间距: 24px
- 背景: `bg-surface`
- 边框: `border-default`
- 选中态: `border-accent`

---

## Inspector 面板

右侧固定宽度 240px。

### 结构

```
┌─────────────────────┐
│  Keyframe Info      │
│  Name / Summary     │
├─────────────────────┤
│  Transform          │
│  X Y W H            │
├─────────────────────┤
│  Fill / Stroke      │
├─────────────────────┤
│  Attributes         │
│  + Add              │
├─────────────────────┤
│  Transitions        │
└─────────────────────┘
```

### 属性行样式

- 行高: 32px
- 标签宽度: 60px
- 输入框: `flex: 1`
- 间距: 8px
